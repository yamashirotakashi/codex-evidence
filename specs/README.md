# Specs

Use spec-first workflow in this repository.

Expected active artifact set:

- `requirements.md`
- `design.md`
- `tasks.yaml`
- `worklog.ndjson`
- `traceability.csv`

Before implementation, use:

- `SDD-TDD-EDD-guideline`
